<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- bootstrap CSS link -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- bootstrap JS script -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Tutoring App</title>
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">Tutoring App</a>
            <!-- navbar toggler for responsive design -->
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="collapsibleNavbar">
                <!-- navbar links -->
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/addStudent">Add new student</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Main content -->
    <div class="container">
        <br>
        <div class="text-center"><h2>Update Student</h2></div>
        <br>
        <div class="text-center">
            <% if (student) { %> 
                <!-- form to edit student information -->
                <form action="/editStudent/<%= student.student_id %>" method="POST" enctype="multipart/form-data">
                    <!-- input fields for student details -->
                    <label for="student_id">Student ID:</label><br>
                    <input type="text" id="student_id" name="student_id" value="<%= student.student_id %>" readonly><br><br>

                    <label for="name">Name:</label><br>
                    <input type="text" id="name" name="name" value="<%= student.name %>" required><br><br>

                    <label for="schedule">Schedule:</label><br>
                    <input type="text" id="schedule" name="schedule" value="<%= student.schedule %>" required><br><br>

                    <label for="points_earned">Points Earned:</label><br>
                    <input type="number" id="points_earned" name="points_earned" value="<%= student.points_earned %>" required><br><br>

                    <label for="subject">Subject:</label><br>
                    <input type="text" id="subject" name="subject" value="<%= student.subject %>" required><br><br>

                    <label for="contact">Contact:</label><br>
                    <input type="text" id="contact" name="contact" value="<%= student.contact %>" required><br><br>

                    <!-- display current image -->
                    <label for="currentImage">Current Image:</label><br>
                    <input type="text" id="currentImage" name="currentImage" value="<%= student.image %>" readonly><br>
                    <img src="/images/<%= student.image %>" class="img-fluid mt-2" style="max-width: 200px;"><br><br>

                    <!-- input field to upload new image -->
                    <label for="image">New Image:</label><br>
                    <input type="file" id="image" name="image" accept="image/*"><br><br>

                    <!-- submit button to update student -->
                    <button type="submit" class="btn btn-primary">Update Student</button>
                </form>
            <% } else { %>
                <!-- message if no student found -->
                <p>No student found.</p>
            <% } %>
            <!-- back button to return to student index -->
            <a href="/index" class="btn btn-secondary mt-3">Back</a>
        </div>
    </div>
</body>
</html>
