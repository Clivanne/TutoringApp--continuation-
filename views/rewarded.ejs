<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <!-- sets the character encoding of the document to UTF-8 -->
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- ensures the webpage is responsive and sets the width to the device width -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- includes Bootstrap CSS for styling -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <!-- includes Bootstrap JavaScript bundle for functionality -->
  <title>Tutoring App - Reward Student</title>
  <!-- sets the title of the webpage -->
</head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
      <!-- creates a dark-themed navbar -->
      <div class="container-fluid">
        <!-- ensures the navbar is fluid and responsive -->
        <a class="navbar-brand" href="/">Tutoring App</a>
        <!-- brand link to the home page -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
          <span class="navbar-toggler-icon"></span>
        </button>
        <!-- toggle button for collapsing the navbar on smaller screens -->
        <div class="collapse navbar-collapse" id="collapsibleNavbar">
          <!-- collapsible part of the navbar -->
          <ul class="navbar-nav">
            <!-- list of navbar items -->
            <li class="nav-item">
              <a class="nav-link" href="/addStudent">Add new student</a>
              <!-- link to add a new student -->
            </li> 
            <li class="nav-item">
              <a class="nav-link" href="/reward">Rewards</a>
              <!-- link to view rewards -->
            </li> 
          </ul>
        </div>
      </div>
    </nav>
  
    <div class="container">
      <!-- container for the main content -->
      <br>
      <div class="text-center"><h2>Reward <%= reward.reward_name %> to a Student</h2></div>
      <!-- header for the reward student section -->
      <br>
    
      <div class="row justify-content-center">
        <!-- centers the form horizontally -->
        <div class="col-md-6">
          <!-- column with medium width (6 out of 12) -->
          <form action="/rewarded/<%= reward.reward_id %>/award" method="POST">
            <!-- form to submit the selected student to be awarded a reward -->
            <div class="mb-3">
              <!-- margin-bottom utility class to add space below the element -->
              <label for="student" class="form-label">Select Student:</label>
              <!-- label for the student selection dropdown -->
              <select class="form-select" id="student" name="studentId" required>
                <!-- dropdown for selecting a student -->
                <option value="">Select a student</option>
                <!-- default option prompting the user to select a student -->
                <% for (let i = 0; i < students.length; i++) { %>
                  <!-- loop through the students array and create an option for each student -->
                  <option value="<%= students[i].student_id %>"><%= students[i].name %></option>
                  <!-- option value is the student ID, displayed text is the student name -->
                <% } %>
              </select>
            </div>
            <button type="submit" class="btn btn-primary">Award</button>
            <!-- submit button to award the selected student -->
          </form>
        </div>
      </div>
    </div>
</body>
</html>
