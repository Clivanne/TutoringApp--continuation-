<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <!-- sets the character encoding of the document to UTF-8 -->
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- ensures the webpage is responsive and sets the width to the device width -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- includes Bootstrap CSS for styling -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <!-- includes Bootstrap JavaScript bundle for functionality -->
    <title>Student Details</title>
    <!-- sets the title of the webpage -->
</head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <!-- creates a dark-themed navbar -->
        <div class="container-fluid">
          <!-- ensures the navbar is fluid and responsive -->
          <a class="navbar-brand" href="/">Tutoring App</a>
          <!-- brand link to the home page -->
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
          <!-- toggle button for collapsing the navbar on smaller screens -->
          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <!-- collapsible part of the navbar -->
            <ul class="navbar-nav">
              <!-- list of navbar items -->
              <li class="nav-item">
                <a class="nav-link" href="/addStudent">Add new student</a>
                <!-- link to add a new student -->
              </li> 
            </ul>
          </div>
        </div>
    </nav>
    
    <div class="container">
        <!-- container for the main content -->
        <br>
        <div class="text-center"><h2>Student Details</h2></div>
        <!-- header for the student details section -->
        <br>
        <% if (student) { %>
            <!-- checks if the student object is available -->
            <div>
                <p><b>Student ID:</b><br> <%= student.student_id %></p>
                <!-- displays the student ID -->
                <p><b>Student Name:</b><br> <%= student.name %></p>
                <!-- displays the student name -->
                <p><b>Student Image:</b><br> <img src="/images/<%= student.image %>" width="20%"></p>
                <!-- displays the student image with a width of 20% -->
                <p><b>Schedule:</b><br> <%= student.schedule %></p>
                <!-- displays the student's schedule -->
                <p><b>Points Earned:</b><br> <%= student.points_earned %></p>
                <!-- displays the points earned by the student -->
                <p><b>Subject:</b><br> <%= student.subject %></p>
                <!-- displays the student's subject -->
                <p><b>Contact:</b><br> <%= student.contact %></p>
                <!-- displays the student's contact information -->
            </div>
        <% } else { %>
            <!-- if no student object is available, display a message -->
            <p>No student found.</p>
        <% } %>
        <a href="/index" class="btn btn-primary mt-3">Back</a>
        <!-- button to navigate back to the index page -->
    </div>
</body>
</html>
